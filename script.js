// function holding the maxArr
function maxArr() {

    // finds the max value from the array
    var arr = Math.max();

    // log the max value
    console.log(arr);
}

// function calls itself
maxArr();