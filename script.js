// function holding the maxArr
function maxArr() {

    // finds the max value from the array
    var arr = Math.max(120, 123, 1, 1200);

    // log the max value
    console.log(arr);
}

// function calls itself
maxArr();